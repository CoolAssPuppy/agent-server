import SwiftUI
import NerdsUI

// MARK: - Nerds (default)

struct NerdsPalette: AppPalette {
    let id = "nerds"
    let displayName = "Nerds"
    let isDark = true
    let primary = Color(hex: "#FDB817")
    let primaryForeground = Color(hex: "#121212")
    let background = Color(hex: "#121212")
    let foreground = Color(hex: "#F0F0F0")
    let card = Color(hex: "#1C1C1C")
    let cardForeground = Color(hex: "#F0F0F0")
    let muted = Color(hex: "#2A2A2A")
    let mutedForeground = Color(hex: "#9A9A9A")
    let accent = Color(hex: "#FCDE09")
    let accentForeground = Color(hex: "#121212")
    let border = Color(hex: "#3A3A3A")
    let destructive = Color(hex: "#EF4444")
}

// MARK: - Starling (Clarice Starling, Silence of the Lambs)

struct StarlingPalette: AppPalette {
    let id = "starling"
    let displayName = "Starling"
    let isDark = true
    let primary = Color(hex: "#00FF41")
    let primaryForeground = Color(hex: "#0D0D0D")
    let background = Color(hex: "#0D0D0D")
    let foreground = Color(hex: "#E0E8E0")
    let card = Color(hex: "#161B16")
    let cardForeground = Color(hex: "#E0E8E0")
    let muted = Color(hex: "#1E261E")
    let mutedForeground = Color(hex: "#7A9A7A")
    let accent = Color(hex: "#00CC33")
    let accentForeground = Color(hex: "#0D0D0D")
    let border = Color(hex: "#2A3A2A")
    let destructive = Color(hex: "#F87171")
}

// MARK: - Scully (Dana Scully, The X-Files)

struct ScullyPalette: AppPalette {
    let id = "scully"
    let displayName = "Scully"
    let isDark = true
    let primary = Color(hex: "#5CB8E4")
    let primaryForeground = Color(hex: "#0A0F14")
    let background = Color(hex: "#0A0F14")
    let foreground = Color(hex: "#D8E8F0")
    let card = Color(hex: "#131D26")
    let cardForeground = Color(hex: "#D8E8F0")
    let muted = Color(hex: "#1A2836")
    let mutedForeground = Color(hex: "#7A9AB0")
    let accent = Color(hex: "#3D9BD4")
    let accentForeground = Color(hex: "#0A0F14")
    let border = Color(hex: "#243848")
    let destructive = Color(hex: "#F87171")
}

// MARK: - Hotchner (Aaron Hotchner, Criminal Minds)

struct HotchnerPalette: AppPalette {
    let id = "hotchner"
    let displayName = "Hotchner"
    let isDark = true
    let primary = Color(hex: "#9B59B6")
    let primaryForeground = Color(hex: "#F5F0F8")
    let background = Color(hex: "#110E1A")
    let foreground = Color(hex: "#E8E0F0")
    let card = Color(hex: "#1A1526")
    let cardForeground = Color(hex: "#E8E0F0")
    let muted = Color(hex: "#241E34")
    let mutedForeground = Color(hex: "#9080A8")
    let accent = Color(hex: "#7E3FA0")
    let accentForeground = Color(hex: "#F5F0F8")
    let border = Color(hex: "#362A4A")
    let destructive = Color(hex: "#F87171")
}

// MARK: - Graff (Carl Hanratty/Graff, Catch Me If You Can)

struct GraffPalette: AppPalette {
    let id = "graff"
    let displayName = "Graff"
    let isDark = false
    let primary = Color(hex: "#D4850A")
    let primaryForeground = Color(hex: "#FFFFFF")
    let background = Color(hex: "#FAF7F0")
    let foreground = Color(hex: "#2A1F0A")
    let card = Color(hex: "#FEFCF6")
    let cardForeground = Color(hex: "#2A1F0A")
    let muted = Color(hex: "#F0EBE0")
    let mutedForeground = Color(hex: "#7A6840")
    let accent = Color(hex: "#E8C878")
    let accentForeground = Color(hex: "#2A1F0A")
    let border = Color(hex: "#E0D8C8")
    let destructive = Color(hex: "#DC2626")
}

// MARK: - Kujan (Dave Kujan, The Usual Suspects)

struct KujanPalette: AppPalette {
    let id = "kujan"
    let displayName = "Kujan"
    let isDark = false
    let primary = Color(hex: "#607D8B")
    let primaryForeground = Color(hex: "#FFFFFF")
    let background = Color(hex: "#F5F5F5")
    let foreground = Color(hex: "#1A1A1A")
    let card = Color(hex: "#FAFAFA")
    let cardForeground = Color(hex: "#1A1A1A")
    let muted = Color(hex: "#ECEFF1")
    let mutedForeground = Color(hex: "#78909C")
    let accent = Color(hex: "#B0BEC5")
    let accentForeground = Color(hex: "#1A1A1A")
    let border = Color(hex: "#CFD8DC")
    let destructive = Color(hex: "#DC2626")
}

// MARK: - Ness (Eliot Ness, The Untouchables)

struct NessPalette: AppPalette {
    let id = "ness"
    let displayName = "Ness"
    let isDark = true
    let primary = Color(hex: "#CD7F32")
    let primaryForeground = Color(hex: "#F5F0EA")
    let background = Color(hex: "#1A1410")
    let foreground = Color(hex: "#E8DDD0")
    let card = Color(hex: "#241C14")
    let cardForeground = Color(hex: "#E8DDD0")
    let muted = Color(hex: "#2E241A")
    let mutedForeground = Color(hex: "#A09080")
    let accent = Color(hex: "#B8860B")
    let accentForeground = Color(hex: "#F5F0EA")
    let border = Color(hex: "#3A3028")
    let destructive = Color(hex: "#F87171")
}
